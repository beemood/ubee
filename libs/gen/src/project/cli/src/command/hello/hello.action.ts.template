import { HelloOptions } from './hello.options.js';

/**
 * Say hello
 * @param options {@link HelloOptions}
 */
export function helloAction(options: HelloOptions): void {
  console.log(options.template.replaceAll('$name', options.name));
}
