{
  "$schema": "https://json-schema.org/schema",
  "$id": "BindCommand",
  "title": "Bind the generator command to the main program. Run this command after generating command.",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What project would you like to use?"
    }
  },
  "required": ["project"]
}
